import './style.less';
import {loadingCircleProps} from './types';

export function LoadingCircle({className} : loadingCircleProps) {
  return (
    <div className={'LoadingCircle ' + className}>
        
    </div>
  );
}